import { HoverSpyDirective } from './hover-spy.directive';

describe('HoverSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverSpyDirective();
    expect(directive).toBeTruthy();
  });
});
